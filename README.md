# Idle Life Bookshelf Simulator

Static prototype for the bookshelf flow, styled from the Figma rework and deployable on GitHub Pages.

## Local preview

Open `index.html` in a browser.

## GitHub Pages

The repository includes a GitHub Actions workflow that deploys the app to GitHub Pages from the `main` branch.
